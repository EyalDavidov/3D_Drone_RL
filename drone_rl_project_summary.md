# סיכום פרויקט: ניווט רחפן אוטונומי בסביבה מרובת מכשולים

מסמך זה מרכז את כל השינויים הטכניים, ההחלטות העיצוביות ופונקציית התגמול שהובילו לאימון מוצלח של מודל הניווט, לקראת פגישת הסטטוס.

---

## 1. המעבר מארכיטקטורת SAC+VAE ל-PPO+AE

### למה שינינו את אלגוריתם הלמידה מ-SAC ל-PPO?
*   **SAC (Soft Actor-Critic)**: אלגוריתם Off-policy ששומר חוויות עבר ב-Replay Buffer וממחזר אותן. בסביבות עשירות במידע (כמו תמונות עומק), הזיכרון מתמלא מהר, קצב האימון מאט משמעותית, והאלגוריתם רגיש מאוד לשינויים ב-Hyperparameters.
*   **PPO (Proximal Policy Optimization)**: אלגוריתם On-policy שנחשב כיום לסטנדרט בתעשייה לרובוטיקה (ובעיקר ב-Isaac Sim/Lab). הוא יציב הרבה יותר בזכות מנגנון ה-Clipping שמונע עדכונים חדים מדי ל-Policy, ומתאים מאוד לאימון מקבילי ומהיר של מאות רחפנים בו זמנית (512 סביבות מקביליות).

### למה שינינו מ-VAE (Variational Autoencoder) ל-AE (Autoencoder)?
*   ה-VAE דוגם מתוך התפלגות (Reparameterization Trick: z = μ + σ·ε), מה שמוסיף **רעש סטטיסטי** לקידוד התמונה בכל צעד.
*   בסביבת ניווט שבה דיוק מרחקי הוא קריטי (רחפן שחולף סנטימטרים מעמוד), הרעש הזה הקשה על הרחפן ללמוד קשר ישיר בין מה שהוא רואה לבין הפעולה שעליו לבצע.
*   המעבר ל-AE רגיל סיפק ייצוג (Latent Space) **דטרמיניסטי** ויציב — אותה תמונה תמיד תייצר את אותו וקטור z, ששיפר דרמטית את יכולת ההתחמקות ממכשולים.

---

## 2. ארכיטקטורת ה-Autoencoder

ה-AE מאומן **מראש** (Pre-trained) על תמונות עומק שנאספו מהסימולטור, ונטען כ-frozen encoder בזמן אימון ה-RL (ללא העברת גרדיאנטים לאנקודר).

### מבנה הרשת:
```
Encoder:
  Input:  (B, 1, 72, 128) — תמונת עומק גווני אפור מנורמלת ל-[0,1]
  Conv2d(1→32,  k=4, s=2, p=1)  + ReLU  → (B, 32,  36, 64)
  Conv2d(32→64, k=4, s=2, p=1)  + ReLU  → (B, 64,  18, 32)
  Conv2d(64→128,k=4, s=2, p=1)  + ReLU  → (B, 128,  9, 16)
  Conv2d(128→256,k=4,s=2, p=1)  + ReLU  → (B, 256,  4,  8)
  Flatten → (B, 8192)
  Linear(8192 → 32)                      → z (B, 32)  — הקוד הלטנטי

Decoder (לצורך אימון בלבד, לא בשימוש ב-RL):
  Linear(32 → 8192) + Reshape → (B, 256, 4, 8)
  4× ConvTranspose2d layers     → (B, 1, 72, 128)  — שחזור התמונה
  Sigmoid → ערכים ב-[0,1]

Loss: MSE בין התמונה המקורית לשחזור (ללא KL — זה ההבדל מ-VAE)
```

---

## 3. מרחב התצפיות (Observation Space) — 45 ממדים

הסוכן מקבל בכל צעד 45 ממדים המרכיבים את תפיסת העולם שלו:

| רכיב | ממדים | תיאור |
|------|-------|-------|
| `z_img` (AE latent) | 32 | ייצוג דחוס של תמונת העומק — מאפשר לרחפן "לראות" מכשולים |
| `desired_pos_b` (target in body frame) | 3 | וקטור הכיוון ליעד **במערכת הצירים של הגוף** (x,y,z) |
| `target_dist` | 1 | מרחק סקלרי ליעד |
| `root_lin_vel_b` | 3 | מהירות קווית של הרחפן (body frame) |
| `root_ang_vel_b` | 3 | מהירות זוויתית של הרחפן (body frame) |
| `projected_gravity_b` | 3 | וקטור הכבידה המוטל על מערכת הגוף — מקודד את הטיה (roll/pitch) בלי ייצוג זוויתי ישיר |
| **סה"כ** | **45** | `32 + 3 + 1 + 3 + 3 + 3` |

> **הערה**: `projected_gravity_b` הוא טריק מקובל ב-RL לרובוטיקה — במקום לייצג זוויות אוילר (שיש בהן בעיות של Gimbal Lock), מציגים את וקטור הכבידה כפי שהרחפן "מרגיש" אותו. כשהרחפן ישר, הוקטור הוא (0,0,-1). כשהוא נטוי, הערכים משתנים.

---

## 4. פונקציית התגמול (Reward Shaping) — המפתח להצלחה

### טבלת משקלים מלאה (ערכי הקונפיגורציה הסופיים):

| שם | משקל | סוג | תיאור קצר |
|----|------|------|-----------|
| `w_goal` | **+300.0** | חיובי | פרס על הגעה ליעד (רדיוס 0.25m) |
| `w_progress` | **+20.0** | חיובי | הקטנת מרחק ליעד בכל צעד |
| `w_heading` | +0.15 | חיובי | חזית הרחפן מכוונת ליעד |
| `w_vel_align` | +0.5 | חיובי | מהירות × cos(זווית ליעד) × (speed/max_speed) |
| `w_forward_speed` | +0.3 | חיובי | מהירות קדימה בגוף × heading alignment |
| `collision_penalty` | **-250.0** | שלילי | עונש חד-פעמי על התנגשות (< 0.15m מעמוד) |
| `w_proximity` | -1.5 | שלילי | עונש ליניארי גדל ככל שמתקרבים לעמוד (רדיוס 0.5m) |
| `w_speed_proximity` | **-4.0** | שלילי | **מהירות × קרבה לעמוד** — מכריח האטה ליד מכשולים |
| `w_time` | -0.06 | שלילי | קנס זמן לכל צעד — מזרז סיום משימה |
| `w_ang_vel` | -0.01 | שלילי | עונש על מהירות זוויתית גבוהה (יציבות) |
| `w_yaw_rate` | -0.1 | שלילי | עונש על סיבוב yaw מיותר |
| `w_action` | -0.005 | שלילי | עונש על גודל הפקודה (חיסכון אנרגיה) |
| `w_action_rate` | -0.02 | שלילי | עונש על שינוי חד בפקודות (תנועה חלקה) |
| `w_sideslip` | -0.2 | שלילי | עונש על מהירות צידית (טיסה "בסחיפה") |
| `stuck` | -0.2 | שלילי | עונש אם הרחפן נעצר למרות שהכיוון נכון |

### פירוט הרכיבים החשובים:

**`w_progress` (+20.0)**: מחושב כ-`prev_dist - curr_dist`. כל צעד שבו הרחפן מתקרב ליעד — הוא מתוגמל. צעד שבו הוא מתרחק — הוא נענש. זה הדלק הבסיסי שמניע את הרחפן קדימה.

**`w_vel_align` (+0.5)**: מחושב כ-`cos_sim(velocity, direction_to_goal) × (speed / max_speed)`. מעודד **טיסה מהירה בכיוון הנכון**: טיסה בכיוון היעד + מהירות גבוהה = תגמול מקסימלי. טיסה מהירה בכיוון לא נכון = עונש.

**`w_speed_proximity` (-4.0)**: **הפריצה המרכזית.** מחושב כ-`speed × proximity_penalty`. הרחפן לומד ש-**מהירות גבוהה ליד עמוד** = כאב. זה גורם לו **להאט** כשהוא מתקרב למעבר צר, מה שנותן לו יותר זמן לתמרן ולהתחמק. 
- **-10.0** (ערך ראשוני): חזק מדי — הקפיא את הרחפן מפחד
- **-2.0**: חלש מדי — לא באמת האט
- **-4.0** (ערך סופי): האיזון הנכון

**`proximity`**: מחושב כ-`max` על כל 6 העמודים של `((0.5 - dist) / 0.5).clamp(min=0)`. ליניארי: 0 ב-0.5m, עולה ל-0.7 ב-0.15m (התנגשות). מספק "תחושת סכנה" רציפה.

**`stuck`**: נדלק כשהתנאים הבאים מתקיימים **יחד**: התקדמות אפסית, מהירות < 0.05, וכיוון נכון ליעד. מונע מצב שבו הרחפן "מפחד" לזוז.

---

## 5. מפת בולטות — Saliency Maps (Explainability)

כדי להוכיח שהמודל באמת משתמש בחיישן העומק (ולא רק ב-state הקינמטי) לקבלת החלטות, פיתחנו כלי ויזואליזציה.

### שיטת SmoothGrad:
1. לוקחים את תמונת העומק המקורית `x` ברזולוציה 128×72
2. מוסיפים **רעש גאוסיאני** (σ=0.1) ליצירת N=15 גרסאות מעט שונות: `x̃ᵢ = x + εᵢ`
3. לכל גרסה, מעבירים דרך ה-Pipeline המלא: `depth → AE encoder → concat(z, state) → Actor → action`
4. מחשבים גרדיאנט (נגזרת חלקית) של ה-action ביחס **לפיקסלים של תמונת העומק**: `∂a/∂x̃ᵢ`
5. **ממצעים** את 15 הגרדיאנטים → מקבלים Heatmap נקי ויציב

### למה SmoothGrad ולא Vanilla Gradient?
Vanilla Gradient (ללא רעש) מייצר Heatmap רועש מלא "אבק מתמטי" (Numerical Dust) — הפיקסלים שמוארים לא בהכרח קשורים למכשולים. SmoothGrad מנקה את הרעש על ידי מיצוע, וההתמקדות בעמודים הופכת ברורה.

### Focus on obstacles:
בנוסף, אנחנו מכפילים את ה-Heatmap ב-inverse של תמונת העומק, כך שפיקסלים של אובייקטים **קרובים** (שערכי העומק שלהם נמוכים = חשוכים) מקבלים חשיבות גבוהה יותר. זה גורם ל-Saliency להתמקד באזורים שבהם יש מכשולים ממשיים.

### מה זה מוכיח?
כשהעמודים מוארים ב-Heatmap, זו **הוכחה חותכת** שהרשת העצבית:
1. מזהה את המכשול הבלתי צפוי דרך תמונת העומק
2. מבססת את החלטת ההיגוי שלה על מיקום המכשול
3. לא מסתמכת רק על המצב הקינמטי (state) שלא כולל מידע על מכשולים

---

## 6. הקבצים המרכזיים שעליהם עבדנו

| קובץ | תפקיד |
|------|--------|
| [ae_ppo_drone_env_cfg.py](file:///D:/isaac/3D_Drone_RL/source/first_drone/first_drone/tasks/direct/navigation_drone/ae_ppo_drone_env_cfg.py) | **קונפיגורציה** — כל משקלי ה-Reward, רדיוסים פיזיקליים, גבולות מהירות, מגבלות פעולה |
| [ae_ppo_drone_env.py](file:///D:/isaac/3D_Drone_RL/source/first_drone/first_drone/tasks/direct/navigation_drone/ae_ppo_drone_env.py) | **הסביבה** — חישוב Observations, Rewards, Termination conditions, מטריקות (collision_rate, goal_rate) |
| [ae.py](file:///D:/isaac/3D_Drone_RL/source/first_drone/first_drone/models/ae.py) | **מודל ה-AE** — הגדרת Encoder (4× Conv2d) + Decoder (4× ConvTranspose2d), 32-dim latent |
| [train_ae.py](file:///D:/isaac/3D_Drone_RL/scripts/train_ae.py) | **אימון ה-AE** — טוען תמונות .npy שנאספו, מאמן MSE loss, שומר checkpoints |
| [train_ppo_ae.py](file:///D:/isaac/3D_Drone_RL/scripts/train_ppo_ae.py) | **אימון ה-RL** — PPO עם RSL-RL, רישום מטריקות מותאמות ל-WandB |
| [play_saliency.py](file:///D:/isaac/3D_Drone_RL/scripts/play_saliency.py) | **ויזואליזציית Saliency** — הרצת מודל + חישוב Heatmap בזמן אמת |

---

## 7. ביצועי המודל הסופי (BEST MODEL — 26-05_16-36)

| מטריקה | ערך |
|--------|-----|
| Mean Reward | **~306** |
| Goal Rate | **~92%** |
| Collision Rate | ~8% |
| Mean Episode Length | ~127 steps |
| Action STD | 0.40 |
| Training Time | ~70 דקות (1500 iterations, 512 envs) |
| Total Steps | ~18.4M |

### סיכום ביצועים לפי רכיב reward:
| רכיב | ערך ממוצע | פרשנות |
|------|-----------|--------|
| goal | +27.7 | 92% מגיעים ליעד |
| progress | +4.2 | התקדמות יציבה |
| vel_align | +4.8 | טיסה מהירה בכיוון הנכון |
| forward_speed | +3.9 | מהירות קדימה גבוהה |
| heading | +1.2 | כיוון טוב |
| collision | -1.88 | ~8% התנגשויות |
| proximity | -1.88 | שמירת מרחק סבירה |
| speed_proximity | -5.39 | האטה ליד עמודים |

---

**לסיכום:** המודל הנוכחי מייצג ארכיטקטורה יציבה (PPO+AE), התנהגות מאוזנת דרך Reward Shaping מתקדם (שמאפשר השתחלות צמודה בין עמודים), ויכולת הסבר (Explainability) דרך Saliency Maps שמוכיחה שהסוכן באמת מבין את סביבתו ומגיב למכשולים.
